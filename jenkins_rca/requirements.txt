fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pyyaml>=6.0
google-generativeai>=0.8.0
anthropic>=0.40.0
openai>=1.0.0
diskcache>=5.6.0
pydantic>=2.0.0
python-dotenv>=1.0.0
boto3>=1.34.0
itsdangerous>=2.2.0
psycopg[binary]>=3.2.0
pgvector>=0.3.0
# LangGraph for ULTIMATUM gate orchestration (Phase 6, on
# feature/jenkins-rca-agent-RAG-LANG branch only). Optional — agent
# loop still works without it; gates.py imports lazily and falls
# back to the imperative gate stack when langgraph isn't installed.
langgraph>=0.2.0
langchain-core>=0.3.0
# Templates — main.py imports jinja2 directly for dashboard / report HTML.
jinja2>=3.1.0
